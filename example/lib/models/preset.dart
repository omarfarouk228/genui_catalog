// ---------------------------------------------------------------------------
// Preset model
// ---------------------------------------------------------------------------

import 'package:flutter/material.dart';

class Preset {
  final String label;
  final IconData icon;
  final String prompt;

  const Preset({required this.label, required this.icon, required this.prompt});
}
