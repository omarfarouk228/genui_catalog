name: genui_catalog
description: Ready-to-use GenUI CatalogItems for building dynamic AI-generated UIs.
version: 0.1.0
homepage: https://github.com/theessential/genui_catalog

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  genui: ^0.8.0
  json_schema_builder: ^0.1.3
  fl_chart: ^1.2.0
  intl: ^0.20.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
